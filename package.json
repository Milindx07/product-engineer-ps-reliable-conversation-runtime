{
  "name": "bounded-conversation-runtime",
  "version": "1.0.0",
  "description": "A bounded TypeScript runtime for one streamed conversational turn with policy, cancellation, timeout, persistence semantics, and inspectable traces.",
  "type": "module",
  "private": true,
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "test": "npm run build && node --test dist/test/*.test.js",
    "benchmark": "npm run build && node dist/src/benchmark.js --iterations 10",
    "demo": "npm run build && node dist/src/cli.js demo",
    "live": "npm run build && node dist/src/live-demo.js",
    "check": "npm test && npm run benchmark"
  },
  "devDependencies": {
    "@types/node": "^26.0.0",
    "typescript": "^5.9.2"
  },
  "engines": {
    "node": ">=22"
  }
}
